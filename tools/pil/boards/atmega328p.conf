# PIL board definition — ATmega328P (AVR8).
# Sourced as shell by tools/pil/run.sh.

ARCH=avr
TOOLCHAIN_FILE=cmake/toolchains/avr-toolchain.cmake
BUILD_DIR=build-test-avr
RUNNER=tools/simavr/run_tests.sh

APT_DEPS="gcc-avr avr-libc binutils-avr simavr"
